var Octo = require('./')
var octo = new Octo({token: process.env['TOKEN']})

octo.repos('philschatz', 'gh-demo').projects.fetch()
.then((res) => {
  // console.log(res)
  return res.items[0].columns.fetch()
  .then((cols) => {
    // console.log(cols)
    return cols.items[0].cards.fetch()
    .then((cards) => {
      console.log(cards)
      return cards.items[0].fetch()
      .then((card) => {
        console.log(card)
      })
    })
  })
  // return res.nextPage.fetch()
  // .then((two) => {
  //   console.log('-----------------------')
  //   // console.log(two)
  // })
})
